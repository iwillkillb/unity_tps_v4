using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerBehaviour : MonoBehaviour
{
    // This is operated by receiving the Player's life or death from the PlayerStats.
    /*
    // Enable input switch
    protected bool enableFunction = true;

    protected virtual void Start()
    {
        // Connect PlayerManager
        PlayerManager.instance.onPlayerDied += OnPlayerDied;
        PlayerManager.instance.onPlayerRevived += OnPlayerRevived;
    }

    protected void OnPlayerDied()
    {
        enableFunction = false;
    }

    protected void OnPlayerRevived()
    {
        enableFunction = true;
    }*/
}
